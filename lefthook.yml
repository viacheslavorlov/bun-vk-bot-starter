pre-commit:
  parallel: true
  commands:
    format:
      run: bun run oxfmt --write {staged_files} && git add {staged_files}
      stage_fixed: true
    typecheck:
      run: bun run tsc --noEmit
      glob: '*.ts'
